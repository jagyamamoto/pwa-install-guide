# 他プロジェクトへ組み込むときの依頼文（プロンプト）

AI（Claude Code / Codex など）にそのまま渡せる形にしてあります。
`【】` の中を自分のプロジェクトの内容に書き換えてから、「---」から下をコピーして貼り付けてください。

---

【プロジェクトのパス】のWebアプリに、**ホーム画面のアイコンにしてもらうための案内**を組み込んでください。
高齢者やスマホに不慣れな人が自力で設置できることが目的です。

## 使う部品

`/Users/JAG/Dropbox/jagworks/shared-pwa-install-guide` に共用部品があります。

**まず `README.md` を必ず全部読んでください。** 実機で測った数値と、失敗して捨てた実装が
書いてあります。ここを読まずにコードだけ写すと同じ失敗を繰り返します。特に:

- iOSの共有シートの上端は**画面の上から42.7%**（iPhone 17e / SE第3世代の実測）。
  案内はこの範囲に収める。
- 手順を**時間で自動的に進めてはいけない**（実機テストで失敗した実装）。
- 配色は**黄色地＋黒文字**。アプリ本体と同じ色にすると「指示」だと気づかれない。

## 手順

1. `src/` の3ファイル（`InstallGuide.tsx` / `strings.ts` / `install-guide.css`）を
   【プロジェクトのパス】の適切な場所へコピーする。React以外の構成なら、
   同じ判定ロジックと同じ文言・同じ配色・同じ制約で作り直してよい。

2. **手順スクリーンショットを、このアプリのために撮り直す。**
   `tools/capture-ios-steps.md` の通りに、Xcodeシミュレータで実機の画面を撮る。
   イラストで再現しないこと。押す場所の周辺だけを切り抜き、赤い印は1枚に1つ。
   必要なのは5枚＋完成アイコン1枚:
   - Safari下部の「•••」/ 共有マーク
   - メニューの「共有」の行
   - 共有シートの「表示を増やす」
   - 共有シートの「ホーム画面に追加」の行
   - 追加確認画面の右上「追加」
   - 完成後のホーム画面アイコン

3. `index.html`（またはhead生成箇所）に次を入れる。
   ```html
   <link rel="manifest" href="/manifest.webmanifest" />
   <link rel="apple-touch-icon" href="/icons/apple-touch-icon.png" />
   <meta property="og:image" content="/icons/icon-192.png" />
   <meta property="og:title" content="【アプリ名】" />
   ```
   `og:image` を入れないと、共有シート上部にページ内の画像（地図など）が拾われます。
   ホーム画面のアイコン名は**4〜7文字程度**にしてください（長いと省略されて見つけられません）。

4. **「追加済みか」の判定をサーバ側に作る。**
   iOSはSafariとホーム画面アプリで保存領域が別なので、端末内の記録では判定できません。
   ホーム画面から起動したときにサーバへ記録し、ブラウザ側はそれを読みます。
   README の「追加済みかの判定」の例（監査ログを使う方法／スキーマ変更なし）に倣ってください。
   【この案件で使えるログイン基盤・DBの名前】

5. **恒久的な入口を2つ用意する。**
   - ×で閉じた**その瞬間**に小さな入口へ切り替わること（リロード待ちにすると詰みます）
   - メニュー（☰など）に「最初の設定」を常設すること

6. 表示言語は `lang` で切り替えます。既定は `ja`。
   `ja-easy`（やさしい日本語）/ `en` / `zh` / `vi` を同梱しています。
   【このアプリで必要な言語】に合わせて設定し、足りない言語は `strings.ts` に足してください。
   文言を足すときは README の「文言のルール」（禁止語のリスト）を守ってください。

## 確認（ここまでやって完了）

- Xcodeシミュレータで **iPhone 17e と iPhone SE 第3世代の両方**を使い、
  設定案内 → 実際に「•••」→「共有」→「表示を増やす」→「ホーム画面に追加」→「追加」まで通す。
- 共有シートを開いた状態で、**案内が隠れずに見えている**ことをスクリーンショットで確認する。
- 一番小さい画面（SE）でも手順すべてが見えること。横スクロールが出ないこと。
- ×で閉じた直後に入口が出ること。メニューからも開けること。
- ホーム画面のアイコンから起動したとき「設定できました」が一度だけ出ること。
- Androidでは何も出ないこと（ChromeのインストールUIに任せる設計です）。
- 型チェックとビルドが通ること。

## 報告

変更点と、判断に迷って選んだところを箇条書きで報告してください。
仕様に関わる判断は勝手に決めず、**Yes/No か 選択肢A/B/C** の形でオーナーに聞いてください。
オーナーは開発者ではないため、専門用語での質問・自由記述の質問は避けてください。
