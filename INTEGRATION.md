# 他のPWAアプリへ組み込むときの依頼文

AI（Claude Code / Codex など）にそのまま渡せます。
`【】` を自分のプロジェクトの内容に書き換えて、「---」から下をコピーしてください。

---

【プロジェクトのパス】のWebアプリに、**ホーム画面のアイコンにしてもらうための案内**を
組み込んでください。高齢者やスマホに不慣れな人が自力で設置できることが目的です。

## 使う部品

`/Users/JAG/Dropbox/jagworks/shared-pwa-install-guide`

**まず README.md を全部読んでください。** 実機で測った数値と、失敗して捨てた実装が
書いてあります。コードだけ写すと同じ失敗を繰り返します。特に次の5つは変更禁止です。

1. 手順を**時間で自動送りしない**（実機で必ずズレる）
2. 帯は共有ボタンと**反対側**に置く（Safariは上・Chromeは下）
3. **全面を1色で塗らない**（ブラウザのバーまで染まり共有ボタンが同化する）
4. 共有シートの題名は `document.title` と `og:title` の**両方**を書き換える
5. その題名は**15文字以内**（超えると「…」で切れる）

## 手順

1. `src/` の3ファイルをコピーする。
   `InstallGuide.tsx` / `strings.ts` / `install-guide.css`
   React以外の構成なら、同じ判定・同じ文言・同じ配色・同じ制約で作り直してよい。

2. `assets/` を公開ディレクトリへコピーし、`assetBase` にその場所を渡す。
   **OSの画面写真は全アプリ共通なので撮り直し不要です。**

3. **`done-icon.png` だけ、このアプリ用に用意する**（完成後のホーム画面アイコンの写真）。
   撮り方は `tools/capture-ios-steps.md`。

4. `index.html` に manifest / apple-touch-icon / og:image / og:title を入れる。
   ⚠ `og:title` は部品が書き換えるので、**必ず置いておく**こと。

5. **「追加済みか」の判定をサーバ側に作る**。iOSはSafariとホーム画面アプリで保存領域が
   別なので端末側では判定できません。READMEの例（監査ログを使う／スキーマ変更なし）に
   倣ってください。【この案件のログイン基盤・DB】

6. **恒久的な入口を2つ**用意する。
   - ×で閉じた**その瞬間**に小さな入口へ切り替わること
   - メニュー（☰など）に「最初の設定」を常設すること

7. 表示言語は `lang` で切り替え（ja / ja-easy / en / zh / vi 同梱）。
   足りない言語を足すときは README の「文言のルール」を守ってください。

## 確認（ここまでやって完了）

- Xcodeシミュレータで **iPhone 17e と iPhone SE 第3世代の両方**を使い、
  設定案内 →「…」→「共有」→「表示を増やす」→「ホーム画面に追加」→「追加」まで通す
- **共有シートを開いた状態で案内が隠れていない**ことをスクリーンショットで確認
- **共有シートの題名が「…」で切れていない**ことを確認（15文字以内）
- **ブラウザのバーが黄色に染まっていない**ことを確認（共有ボタンが見えるか）
- 一番小さい画面（SE）でも手順すべてが見え、横スクロールが出ないこと
- ×で閉じた直後に入口が出ること／メニューからも開けること
- ホーム画面から起動したとき「設定できました」が一度だけ出ること
- Androidでは1タップのボタンだけが出ること（長い手順を出さない）
- 型チェックとビルドが通ること
- ⚠ `public/` に検証用HTMLを残さないこと（そのまま本番公開されます）

## 報告

変更点と、判断に迷って選んだところを箇条書きで報告してください。
仕様に関わる判断は勝手に決めず、**Yes/No か 選択肢A/B/C**（各選択肢に「推奨」と理由）で
オーナーに聞いてください。オーナーは開発者ではないので、専門用語での質問は避けてください。
